// Only including this file to be able to lint files excluded in tsconfig.json
// We can remove this file if we eventually remove excluded files from tsconfig.json
{
  "extends": "./tsconfig.json",
  "include": ["next-env.d.ts", "src/**/*", "middleware.ts"],
  "exclude": ["node_modules"]
}
